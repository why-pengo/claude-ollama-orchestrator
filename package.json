{
  "name": "claude-ollama-orchestrator",
  "version": "1.1.0",
  "description": "Routes simple tasks to local Ollama, complex tasks to Claude Code",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^9.26.0",
    "eslint": "^9.26.0",
    "prettier": "^3.5.3"
  },
  "dependencies": {
    "better-sqlite3": "^12.9.0",
    "ink": "^7.0.2",
    "react": "^19.2.6"
  }
}
